import { declareComponent } from "@webflow/react";
import { FooterWrapper } from "./FooterWrapper";

export default declareComponent(FooterWrapper, {
  name: "FooterWrapper",
  group: "Memorial Site",
  description: "Footer wrapper component with CTA links.",
});
