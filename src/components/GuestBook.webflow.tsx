import { declareComponent } from "@webflow/react";
import { GuestBook } from "./GuestBook";

export default declareComponent(GuestBook, {
  name: "GuestBook",
  group: "Memorial Site",
  description: "Guest book form and entries display.",
});
