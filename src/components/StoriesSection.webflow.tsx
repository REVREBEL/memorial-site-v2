import { declareComponent } from "@webflow/react";
import { StoriesSection } from "./StoriesSection";

export default declareComponent(StoriesSection, {
  name: "StoriesSection",
  group: "Memorial Site",
  description: "Stories section with carousel.",
});
