import { declareComponent } from "@webflow/react";
import { MemoryWall } from "./MemoryWall";

export default declareComponent(MemoryWall, {
  name: "MemoryWall",
  group: "Memorial Site",
  description: "Memory wall with submission modal and filters.",
});
