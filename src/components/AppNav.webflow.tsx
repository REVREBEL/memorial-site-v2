import { declareComponent } from "@webflow/react";
import { AppNav } from "./AppNav";

export default declareComponent(AppNav, {
  name: "AppNav",
  group: "Memorial Site",
  description: "Top navigation bar with site links and icons.",
});
