import { declareComponent } from "@webflow/react";
import { Stories } from "./Stories";

export default declareComponent(Stories, {
  name: "Stories",
  group: "Memorial UI",
  description: "Stories section with interactions.",
});
