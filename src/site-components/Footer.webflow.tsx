import { declareComponent } from "@webflow/react";
import { Footer } from "./Footer";

export default declareComponent(Footer, {
  name: "Footer",
  group: "Memorial UI",
  description: "Static footer layout.",
});
