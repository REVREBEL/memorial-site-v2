import { declareComponent } from "@webflow/react";
import { PhotoWall } from "./PhotoWall";

export default declareComponent(PhotoWall, {
  name: "PhotoWall",
  group: "Memorial UI",
  description: "Photo wall layout.",
});
